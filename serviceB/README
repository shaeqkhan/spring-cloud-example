# Spring Cloud - Microservice B

Run this project as a Spring Boot app (e.g. import into IDE and run main method, or use mvn spring-boot:run). 
It will start up on port 8081 and register with Eureka.

http://localhost:8081/serviceB


## Resources

| Path             | Description  |
|------------------|--------------|
| /serviceB | application context |
| /serviceB/health | Displays the health status of the service |
